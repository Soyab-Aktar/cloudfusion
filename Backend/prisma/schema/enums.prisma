enum Role {
  ADMIN
  USER
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum StorageProvider {
  GOOGLE_DRIVE
  AWS_S3
  DROPBOX
  ONEDRIVE
}

enum AccountStatus {
  CONNECTED
  DISCONNECTED
  EXPIRED
  ERROR
}
